apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: helix
  namespace: helix
spec:
  ingressClassName: nginx
  rules:
    - host: helix.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
